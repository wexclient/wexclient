<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

 	<beans:import resource="classpath:persistence-context.xml" />

	<context:annotation-config />
   	<context:component-scan base-package="com.livngroup.gds" />

	<beans:bean id="wexUser" class="com.livngroup.gds.domain.WexUser" scope="singleton">
	    <beans:property name="orgGroupLoginId" value="${org-group-login-id}" />
	    <beans:property name="username" value="${username}" />
	    <beans:property name="password" value="${password}" />
	</beans:bean>

	<beans:bean id="wexUserToken" class="com.livngroup.gds.domain.WexUserToken" scope="singleton">
	    <beans:property name="iPAddress" value="${ipaddress}" />
	    <beans:property name="orgGroupLoginId" value="${org-group-login-id}" />
	    <beans:property name="username" value="${username}" />
	    <beans:property name="password" value="${password}" />
	</beans:bean>

	<beans:bean id="purchaseLogServiceStub" class="com.aocsolutions.encompasswebservices.PurchaseLogServiceStub" scope="singleton">
	    <beans:constructor-arg name="targetEndpoint"  value="${purchase-log-service-target-endpoint}" />
	</beans:bean>


</beans:beans>